public class Ternary_Operator_Eg3 {
    public static synchronized void main(String[] args) {
        int a =10,b =20;
        /* byte c = (a>b)?30:40;
       byte d = (a<b)?30:40;
        System.out.println(c);
        System.out.println(d);

        if number literals are used in a expression compiler by default treats them as int. compiler checks whether expression
        result can be stored in  the operand datatype or not. if the result number literal is in the range of the operand data type
        then no compile time error, otherwise compile time error.

        if operands are used in the expression then the result will be in the higher datatype operand of the expression.
        and the L.H.S operand datatype should also be capable of holding the result datatype, else  compile time error.

         */

        int e = 20;
        int f = 34;



    }
}
