public class Variable_Declaration {

    public static void main(String[] args) {

        int a, b = 10;
        // Valid declaration
        // a is only declared (not initialized)
        // b is declared and initialized with value 10
        // Java allows multiple variables of the SAME data type in a single statement

        /*
        int c = 10, boolean f = true;
        // ❌ Invalid syntax
        // Reason:
        // Java does NOT allow declaring variables of DIFFERENT data types
        // in the same declaration statement

        int d = 10, int e = 20;
        // ❌ Invalid syntax
        // Reason:
        // The data type should be written ONLY ONCE
        // It cannot be repeated for each variable
        // Correct form would be:
        // int d = 10, e = 20;
        */

    }
}
